apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: flask-microservice
  namespace: flask-dev
spec:
  interval: 5m
  releaseName: flask-microservice
  chart:
    spec:
      chart: ./charts/flask-microservice
      sourceRef:
        kind: GitRepository
        name: flask-microservice
        namespace: flux-system
      interval: 1m
  values:
    replicaCount: 1
    image:
      repository: docker.io/topaz82/flask-microservices
      tag: "REPLACE_ME_SHA"   # {"$imagepolicy": "flux-system:flask-microservice:tag"}
    service:
      type: LoadBalancer
    autoscaling:
      enabled: false